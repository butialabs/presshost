# Custom server-level NGINX configuration.
# This file is intentionally empty and can be replaced via Docker volume mount.
#
# Example — reverse proxy at /i/:
#
#   location /i/ {
#       proxy_pass http://image-service:3000/;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#   }
#
# Mount: -v /host/custom-presshost.conf:/etc/nginx/conf.d/custom-presshost.conf
