# Custom HTTP-level NGINX configuration.
# This file is intentionally empty and can be replaced via Docker volume mount.
#
# Example — add a custom upstream:
#
#   upstream my_service {
#       server 127.0.0.1:3000;
#   }
#
# Mount: -v /host/custom-nginx.conf:/etc/nginx/conf.d/custom-nginx.conf
